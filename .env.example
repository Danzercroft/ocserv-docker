# Minimal OCServ .env example

# Metrics settings
ENABLE_METRICS=true
METRICS_PORT=8000
METRICS_INTERVAL=30

# Certificate paths (defaults are for Docker Swarm secrets)
# SERVER_CERT_PATH=/run/secrets/server_cert
# SERVER_KEY_PATH=/run/secrets/server_key  
# PASSWD_PATH=/run/secrets/ocserv_passwd
